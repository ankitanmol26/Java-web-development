1. What is an Execution Context?
ANS: where the js executes the code

2. Why does JavaScript have two phases?
ANS: it has two phases one is memory phase and another is execution phase

3. What happens during the Memory Creation Phase?
ANS: the memry phase allocates the memory for the created content

4. What happens during the Execution Phase?
ANS: in the execution phase it executes the content which is in the memory phase

5. What is the Call Stack?
ANS: it keeps the track of function calls 

6. Why is JavaScript called single-threaded?
ANS: coz it has one call stack and executes one task at a time.

7. In the function chain A → B → C, which function finishes first and why?
ANS: c then b and then a coz it follows stack principle lifo last in first out