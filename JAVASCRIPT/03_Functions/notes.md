functions is reusable block of code which can be reused again and again when u want.
another def:
A function is a reusable unit of behavior that accepts inputs, performs work, and optionally returns a result.


