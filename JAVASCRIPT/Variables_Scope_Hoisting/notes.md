we use 3 variables decleration 
-var
-let
-const

var is used in previous times but now no one uses its coz it has many disadvantages , and we can redeclare it and many dis things it have

let - it is used when we have to constantly change the values 

const- it cannot be changed . it is fixed once declared cannot be changed again.
